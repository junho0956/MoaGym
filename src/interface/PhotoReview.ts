export interface PhotoReviewItemComponent{
    reviewId:number,
    reviewImgUrl:string,
    autorInfo:string,
    reviewPoint:number,
    // reviewInfo:string, 리뷰 아이템 구분할 수 있는지 아직 회의 안됨
    reviewDesc:string,
    reviewCreateAt:string,
}

export interface PhotoReviewListComponent{
    
}